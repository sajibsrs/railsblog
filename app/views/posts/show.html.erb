<h1 class="h2 d-inline-block align-middle"><%= @post.title %></h1>
<%= link_to 'Edit post', edit_post_path, :class => "btn btn-primary float-right" %>
<hr class="mb-4">

<p>By <%= @post.user.fname + ' ' + @post.user.lname %></p>
<p><%= @post.content %></p>

<%= link_to "All posts", posts_path, :class => "btn btn-secondary" %>
