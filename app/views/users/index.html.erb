<% if user_signed_in? %>
  <h1>Users</h1>
  <p>List of users</p>
  <ol>
    <% @users.each do |user| %>
      <li><p><%= link_to user.fname + " " + user.lname, user %></p></li>
    <% end %>
  </ol>
<% end %>
